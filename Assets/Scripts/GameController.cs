using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
using UnityStories;

public class GameController : MonoBehaviour
{
//     public StoriesHelper storiesHelper;
//     static float count;
//     static float nextTick=0;
//     [SerializeField] public const float tickInterval = 2;

//    public  static GameDataManager gameDataManager;

//     // Start is called before the first frame update
//     GameController()
//     {
//         // count = 0;
//         gameDataManager = GameDataManager.GetInstance();
//         storiesHelper.Dispatch(TimedActionsStory.AddTimedActiontFactory.Get(DoorStory.ToggleDoorFactory.Get(Door.Right), 1));

//         // )   .ToggleDoorFactory.Get(_door));

//         // storiesHelper.Dispatch()

//     }

//     // Update is called once per frame
//     void Update()
//     {
//         count+=Time.deltaTime;

//         if(count> nextTick) {
//             // gameDataManager.Tick();

//             storiesHelper.Dispatch(TimedActionsStory.TickFactory.Get());

//             // AddTimedActiontFactory.Get(DoorStory.ToggleDoorFactory.Get(Door.Right), 1));

//             nextTick = count + tickInterval;
//         }
//     }
}
